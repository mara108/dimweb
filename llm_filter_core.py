"""
Общее ядро LLM-фильтрации кандидатов-диминутивов.

Вынесено отдельно от конкретного провайдера (gpt-4o-mini / GigaChat / ...),
чтобы при сравнении моделей единственной переменной была сама модель,
а не ещё и разный промпт или разная логика валидации ответа.

Конкретные обёртки (filter_gpt.py, filter_gigachat.py) реализуют только
call_fn(system_prompt, user_prompt) -> raw_text_response и вызывают
filter_candidates() отсюда.
"""

import json
import re


SYSTEM_PROMPT = """Ты — опытный лексикограф, специализируешься на живом
русском языке и словообразовании, в частности на уменьшительно-ласкательных
формах существительных (диминутивах). Твоя задача — из списка автоматически
сгенерированных кандидатов отобрать только те формы, которые реально
существуют и употребляются КАК ДИМИНУТИВЫ ИМЕННО ОТ ДАННОГО ИСХОДНОГО
СЛОВА (в живой речи, разговорном стиле, художественной литературе — не
обязательно в словаре литературной нормы, просторечные и диалектные формы
тоже считаются существующими).

КРИТИЧЕСКИ ВАЖНО — ловушка омографов: формальное совпадение с реально
существующим русским словом НЕ означает, что это диминутив от заданной
леммы. Пример: для леммы "конь" кандидат "конец" совпадает по написанию
с реальным русским словом — но это слово означает "окончание, край" и
никак не связано по смыслу с "конь", это НЕ диминутив коня, а случайный
омограф. Такую форму нужно отклонить (существует=false), несмотря на то,
что строка "конец" — реальное русское слово. Проверяй не только "есть ли
такое слово в языке", а "понимается ли оно носителями именно как
уменьшительная форма от этого конкретного существительного".

Чтобы не путать реальное слово-омограф с настоящим диминутивом, для
КАЖДОГО кандидата сначала явно сформулируй его значение и словообразовательную
связь с леммой (поле "значение" в ответе), и только после этого выноси
вердикт "существует". Если, формулируя значение, ты понимаешь, что оно
не связано со смыслом леммы (как "конец" не связано с "конь") — это
явный сигнал поставить существует=false, даже если сама строка — известное
тебе слово.

Обрати особое внимание на чередования согласных перед суффиксом
(рука -> руч-, нога -> нож-, муха -> муш-): формы БЕЗ нужного чередования
обычно не существуют (например, "рукочка" неверно, правильно "ручечка"
или "ручка"), а формы С чередованием, в том числе неожиданные на первый
взгляд, часто вполне реальны и употребимы (ручонка, ручища — не путай
разговорность/просторечность с несуществованием).

Отвечай ТОЛЬКО в формате JSON, без какого-либо текста до или после.
Не используй markdown-разметку (никаких ```json блоков)."""


FEW_SHOT_EXAMPLES = [
    {"lemma": "доска", "form": "дощечка", "meaning": "маленькая доска", "exists": True, "connotation": "ласкательное"},
    {"lemma": "край", "form": "краешек", "meaning": "маленький край, кончик", "exists": True, "connotation": "ласкательное"},
    {"lemma": "дождь", "form": "дождик", "meaning": "небольшой, лёгкий дождь", "exists": True, "connotation": "нейтральное"},
    {"lemma": "дождь", "form": "дождчик", "meaning": "не существует, нарушена фонетика", "exists": False, "connotation": None},
    {"lemma": "актриса", "form": "актриска", "meaning": "актриса, с оттенком пренебрежения к её мастерству", "exists": True, "connotation": "пренебрежительное"},
    {"lemma": "актриса", "form": "актрисулька", "meaning": "актриса, снисходительно-пренебрежительно", "exists": True, "connotation": "пренебрежительное"},
    # ниже — примеры, добавленные после разбора конкретных ошибок фильтра
    # на паре "рука": модель путала формы с чередованием и без
    {"lemma": "рука", "form": "рукочка", "meaning": "не существует, нарушено чередование к->ч", "exists": False, "connotation": None},
    {"lemma": "рука", "form": "ручечка", "meaning": "маленькая рука, с нежностью", "exists": True, "connotation": "ласкательное"},
    {"lemma": "рука", "form": "ручонка", "meaning": "маленькая (обычно детская) рука, с пренебрежением/снисходительностью", "exists": True, "connotation": "пренебрежительное"},
    {"lemma": "рука", "form": "ручища", "meaning": "большая, крупная рука", "exists": True, "connotation": "нейтральное"},
    # пример ловушки омографа: "конец" — реальное русское слово, но НЕ
    # диминутив от "конь" (не связано по смыслу), поэтому отклоняется,
    # несмотря на формальное существование как отдельного слова
    {"lemma": "конь", "form": "конец", "meaning": "окончание, завершение чего-либо — НЕ связано по смыслу с 'конь', случайное совпадение написания", "exists": False, "connotation": None},
    {"lemma": "конь", "form": "конёк", "meaning": "маленький конь, лошадка; также любимое увлечение", "exists": True, "connotation": "ласкательное"},
]


def build_user_prompt(lemma: str, candidates: list[dict]) -> str:
    """
    candidates — вывод generator.gen_candidates(lemma):
    [{"form": ..., "suffix": ..., "connotation_hint": ..., "note": ...}, ...]
    """
    examples_block = "\n".join(
        f'  {{"лемма": "{e["lemma"]}", "форма": "{e["form"]}", '
        f'"значение": {json.dumps(e["meaning"], ensure_ascii=False)}, '
        f'"существует": {str(e["exists"]).lower()}, '
        f'"коннотация": {json.dumps(e["connotation"], ensure_ascii=False)}}}'
        for e in FEW_SHOT_EXAMPLES
    )

    candidates_block = "\n".join(
        f'  - "{c["form"]}" (предварительная коннотация по суффиксу: {c["connotation_hint"]})'
        for c in candidates
    )

    return f"""Примеры корректной разметки (для калибровки, не связаны с текущим словом):
{examples_block}

Существительное: "{lemma}"

Кандидаты диминутивных форм (сгенерированы автоматически правилами,
среди них есть реально существующие и есть некорректные/несуществующие):
{candidates_block}

Для КАЖДОГО кандидата из списка выше верни объект со следующими полями,
СТРОГО В ЭТОМ ПОРЯДКЕ (сначала значение, потом вердикт — важно рассуждать
до, а не после):
- "форма": сама форма (строка, точно как в списке кандидатов, без изменений)
- "значение": кратко (несколько слов), что означает эта форма и как она
  словообразовательно связана с леммой "{lemma}" — или "не существует,
  <причина>", если формы нет в языке
- "существует": true/false — существует ли эта форма именно КАК ДИМИНУТИВ
  ОТ ДАННОГО СЛОВА "{lemma}" (а не просто как произвольное русское слово —
  если в поле "значение" ты описал смысл, не связанный с леммой, то это
  омограф, а не диминутив, и здесь должно быть false)
- "коннотация": одно из "нейтральное", "ласкательное", "пренебрежительное"
  (если существует), или null (если существует=false).
  Предварительная коннотация по суффиксу — это только подсказка,
  ты можешь её изменить, если для конкретного слова оттенок другой
  (один и тот же суффикс может давать разную окраску в зависимости от базы).

Верни JSON-объект строго такого вида (ключ "результаты" обязателен):
{{"результаты": [
  {{"форма": "...", "значение": "...", "существует": true, "коннотация": "..."}},
  ...
]}}"""


def strip_code_fences(text: str) -> str:
    """На случай если модель обернула ответ в ```json ... ```."""
    text = text.strip()
    text = re.sub(r"^```(?:json)?\s*", "", text)
    text = re.sub(r"\s*```$", "", text)
    return text.strip()


VALID_CONNOTATIONS = {"нейтральное", "ласкательное", "пренебрежительное"}


def validate_and_normalize(parsed: dict, original_forms: set[str]) -> list[dict]:
    """
    Не доверяем структуре ответа вслепую (ни json_object, ни промпт-based
    JSON без строгой схемы этого не гарантируют). Проверяем каждое поле
    и отбрасываем некорректные записи вместо падения всего пайплайна
    на одном плохом ответе.
    """
    results = parsed.get("результаты")
    if not isinstance(results, list):
        raise ValueError(f"Ожидался список в поле 'результаты', получено: {type(results)}")

    validated = []
    for item in results:
        if not isinstance(item, dict):
            continue
        form = item.get("форма")
        exists = item.get("существует")
        connotation = item.get("коннотация")
        meaning = item.get("значение")  # необязательное поле-обоснование
        # (chain-of-thought), не влияет на валидацию — модель может его
        # пропустить без ошибки, но обычно присутствует и полезно для
        # отладки/будущего показа лингвисту при экспертной верификации

        if not isinstance(form, str) or form not in original_forms:
            continue  # модель могла придумать форму не из списка — отбрасываем
        if not isinstance(exists, bool):
            continue

        if exists and connotation not in VALID_CONNOTATIONS:
            connotation = None  # модель дала невалидную коннотацию — не выдумываем
        if not exists:
            connotation = None

        validated.append({
            "form": form,
            "exists": exists,
            "connotation": connotation,
            "meaning": meaning if isinstance(meaning, str) else None,
        })

    return validated


def filter_candidates(call_fn, lemma: str, candidates: list[dict], max_retries: int = 2) -> list[dict]:
    """
    Провайдеро-независимая логика фильтрации.

    call_fn: callable(system_prompt: str, user_prompt: str) -> str
        Обёртка над конкретным API (OpenAI через LangChain, GigaChat через
        requests, ...), которая просто возвращает сырой текстовый ответ модели.

    Возвращает список:
    [{"form": ..., "suffix": ..., "exists": ..., "connotation": ..., "note": ...}, ...]

    Формы, которые не удалось корректно оценить (сбой парсинга после всех
    попыток), возвращаются с exists=None — явный сигнал "не проверено",
    а не тихая потеря данных.
    """
    if not candidates:
        return []

    original_forms = {c["form"] for c in candidates}
    user_prompt = build_user_prompt(lemma, candidates)

    last_error = None
    validated = None
    for attempt in range(max_retries + 1):
        try:
            raw_response = call_fn(SYSTEM_PROMPT, user_prompt)
            raw = strip_code_fences(raw_response)
            parsed = json.loads(raw)
            validated = validate_and_normalize(parsed, original_forms)
            break
        except (json.JSONDecodeError, ValueError, KeyError) as e:
            last_error = e
            validated = None

    if validated is None and last_error is not None:
        print(f"[llm_filter_core] Не удалось получить корректный ответ для '{lemma}' "
              f"после {max_retries + 1} попыток: {last_error}")

    by_form = {v["form"]: v for v in validated} if validated else {}

    result = []
    for c in candidates:
        llm_result = by_form.get(c["form"])
        result.append({
            "form": c["form"],
            "suffix": c["suffix"],
            "exists": llm_result["exists"] if llm_result else None,
            "connotation": llm_result["connotation"] if llm_result else None,
            "meaning": llm_result.get("meaning") if llm_result else None,
            "note": c["note"],
            "in_opencorpora": c.get("in_opencorpora", False),
        })

    return result
